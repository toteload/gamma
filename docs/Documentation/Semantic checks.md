Documentation of all the semantic checks done by the compiler.

- Can only use `break` and `continue` inside `loop`
- Program must have `main` function
- Symbols must be declared before they are used


# Backlog
- 
