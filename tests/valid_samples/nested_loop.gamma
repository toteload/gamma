fn main() -> int {
    let i: int
    set i = 0

    loop {
        if (eq i 10) {
            break
        }

        let j: int
        set j = 0

        loop {
            if (eq j 10) {
                break
            }

            set j = (+ j 1)
        }

        set i = (+ i 1)
    }

    return 0
}
