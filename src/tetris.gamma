cfn SDL_CreateWindow(title: *u8, x: i32, y: i32, w: i32, h: i32, flags: u32): *void
cfn SDL_DestroyWindow(window: *void): void

fn main(): int
  let SDL_WINDOWPOS_CENTERED: i32 = 1

  let window: *void = (SDL_CreateWindow 0 Centered Centered 800 800 Flags)

  (SDL_DestroyWindow window)
  
  return 0
end
